//! FFI preparation crate for runar-keys
//!
//! This crate currently holds design docs and feature flags for the future FFI implementation.
//! Implementation will provide a C ABI exposing selected functions from `runar-keys` for Swift/Kotlin.

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {
        // assert!(true);
    }
}
