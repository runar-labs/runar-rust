We need to test the discovery APIS exposed in the FFI crate.

We need a test in teh FFI taht is equivalent to the test /Users/rafael/dev/runar-rust/runar-node-tests/src/network/multicast_discovery_test.rs

That tests all the same steps.. same steup. but using FFI API.. to make sure we have all the proper Discovery APIS exposed in the FFI crate and that they work as intended.